@model OrderModel

@{
    ViewData["Title"] = "RoomOrderView";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<div class="container-fluid">
    <div class="row">
        @for (int i = 0; i < Model.Rooms.Count; i++)
        {
            <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 py-2">

                <div class="content alert-info" style="border : solid 2px black;">
                    <div>
                        <dl style="margin: 0px 10px 0px 0px;">
                            <dt>
                                @Html.DisplayNameFor(m => m.Rooms[i].Number)
                            </dt>
                            <dd>
                                @Model.Rooms[i].Number
                            </dd>
                            <dt>
                                @Html.DisplayNameFor(m => m.Rooms[i].PricePerNight)
                            </dt>
                            <dd>
                                @Model.Rooms[i].PricePerNight
                            </dd>

                            <dt>
                                @Html.DisplayNameFor(m => m.Rooms[i].Rented)
                            </dt>
                            @if (Model.Rooms[i].Rented == true)
                            {
                                <dd>
                                    Ja
                                </dd>
                            }
                            else
                            {
                                <dd>
                                    Nej
                                </dd>
                            }
                        </dl>
                        <!-- Made ready for order
                        <ul style="display: inline-block; list-style-type:none;">
                            <li style="margin-left: 10px; display: inline-block; list-style-type: none;">
                                using (Html.BeginForm("EditCourse", "Course", FormMethod.Get))
                                {
                                    //Html.ValidationSummary(true)
                                    <input asp-for="Id" type="number" value="//Model.Courses[i].Id" hidden />
                                    <button type="submit">Rediger</button>
                                }

                            </li>
                            <li style="margin-left: 10px; display: inline-block; list-style-type: none;">
                                using (Html.BeginForm("DeleteCourse", "Course", FormMethod.Get))
                                {
                                    //Html.ValidationSummary(true)
                                    <input asp-for="Id" type="number" value="//Model.Courses[i].Id" hidden />
                                    <button type="submit">Fjern</button>
                                }

                            </li>
                        </ul>

                        -->
                    </div>
                </div>
            </div>
        }
    </div>

</div>